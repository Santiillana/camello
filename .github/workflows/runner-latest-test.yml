name: Prueba runner latest temporal
on:
  push:
    branches: ["fix/sqlite-wasm-web"]
permissions:
  contents: read
jobs:
  runner:
    runs-on: ubuntu-latest
    steps:
      - run: echo RUNNER_LATEST_OK
